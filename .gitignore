# This is a Git ignore file for Go projects.
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/

# Go workspace file
go.work

# Local Envrionment
.direnv
.devenv*
devenv.local.nix
.env
coverage
tags

# Test reports
reports

# Build artifacts
build
dist

# IDE configuration files
.vscode
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
